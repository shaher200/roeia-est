
import { registerPlugin } from '@capacitor/core';

// تسجيل إضافات Capacitor هنا إذا لزم الأمر
export const initializeCapacitor = () => {
  // يمكن إضافة تكوينات إضافية هنا
  console.log('Capacitor initialized for mobile app');
};
